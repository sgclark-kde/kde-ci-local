Slave are auto generated by prepare_slaves on Jenkins.
Preparation on slave machine includes:
Add jenkins to docker group.
sudo gpasswd -a jenkins docker
sudo systemctl restart docker 
newgrp jenkins

Run prepare_slaves in jenkins.

On first run for a new slave the data volume containers. will need to be created.

Run on the slave docker host 
./run-data-volumes-on-the-slave-host.sh